import { DashboardContainer } from "@/presentation/containers/DashboardContainer";

export default function Dashboard() {
  // Use container component that handles all business logic and state management
  return <DashboardContainer userId={1} />;
}
